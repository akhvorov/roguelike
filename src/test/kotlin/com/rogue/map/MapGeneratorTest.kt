package com.rogue.map

import com.rogue.utils.Point
import org.junit.jupiter.api.Assertions.assertTrue
import org.junit.jupiter.api.Test

class MapGeneratorTest {
//    @Test
//    fun testGeneratedMap() {
//        val levelMap = MapGenerator.generateInitialMap()
//        val freePoints = levelMap.getFree()
//        for (cell in levelMap.cells) {
//            assertTrue(havePathToPlayer(freePoints, cell.point))
//        }
//    }
//
//    private fun havePathToPlayer(points: Set<Point>, fromPoint: Point, toPoint: Point): Boolean {
//        val queue = listOf(fromPoint)
//        while (queue.isNotEmpty()) {
//            val point = queue[0]
//
//        }
//    }
}